#![forbid(unsafe_code)]
//! Identify by mac: reads the peer's link-layer address, where the transport reports one; a
//! transport-layer identifier whose claim is passed.
//!
//! Declared and not yet written: `architecture.toml` carries the maturity. When it
//! is, it implements `TransportIdentifier` (ADR-0050).
